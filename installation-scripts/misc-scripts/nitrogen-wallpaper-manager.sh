#!/usr/bin/env bash

# Install nitrogen, a wallpaper manager.
sudo apt install nitrogen

# # # This command should be placed in the i3 config. Alternatively, this can
# # # be placed within a startup script, but this can cause problems when using
# # # tmux etc.
# nitrogen --restore &

