#!/bin/zsh

# # # This script contains aliases for Git.
# -----------------------------------------------------------------------------

# git status aliases
alias gs="git status"
alias gsv="git status -v -v"

# git diff aliases
alias gdc="git diff --cached"
